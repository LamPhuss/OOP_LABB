Q1:
What happens if users choose “Cancel”?
Ans:
Program will return "No"

Q2:
How to customize the options to users, e.g. only two options: “Yes” and “No”, OR “I do”
and “I don’t” (Suggestion: Use Javadocs or using Eclipse/Netbean IDE help)
Ans: 
int response = JOptionPane.showOptionDialog(null, "Do you want to change to the first class ticket?", "Confirmation", JOptionPane.YES_NO_OPTION, JOptionPane.QUESTION, null, null, null);
if (response == JOptionPane.YES_OPTION) {
} else if (response == JOptionPane.NO_OPTION) {
}